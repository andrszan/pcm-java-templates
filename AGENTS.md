# Java 项目仓库协作规则

## 语言规则

- 面向用户和维护者的文档、说明、注释和 Git 提交说明默认使用中文。
- 文件名、命令、配置键、代码标识符、框架名和通用技术名词可以保留英文。
- 除非用户明确要求，不要自行把仓库说明改成英文。

## 修改规则

- `main` 是仓库默认开发分支，日常直接在 `main` 修改对应项目目录；不要为每个项目维护长期开发分支。
- 需要独立评审、并行开发或隔离较大变更时，才创建短期分支；完成后合并回 `main`，不把短期分支当作项目的固定开发入口。
- 默认只修改用户指定的项目目录；不得在项目之间建立依赖。
- 不得增加仓库根目录共享包、共享构建脚本或工作区依赖。
- 每个项目必须能够独立安装、运行、测试和构建。
- 项目目录可以作为基础架构起点，但项目内部文档和规则应描述当前项目的技术选型与开发方式，不把仓库的模板管理概念带入项目开发语境。
- 不得加入未经任务确认的客户业务模型、接口、流程或示例数据。
- 不因仓库中存在某个依赖或基础设施就在其他项目中默认采用；各项目按自身 README、构建声明和实际需求独立演进。
- 完成修改前，必须执行受影响项目声明的本地验证命令；涉及数据库、迁移或外部服务时，明确报告真实集成验证结果和未验证部分。

## 目录约定

- 正式项目目录位于 `templates/<project-id>/`；目录名称是仓库组织方式，不改变项目内部的开发语境。
- 项目不得依赖仓库根目录文件、其他项目或开发者机器上的私有文件。
- 项目的 Java 版本、构建工具、依赖、运行方式和验证命令以项目自身的 `README.md`、`pom.xml` 或 `build.gradle(.kts)` 及对应 Wrapper 为准。
- 项目需要环境变量时提供无秘密的 `.env.example`；真实 `.env`、IDE 配置、构建产物和本地缓存不得提交。
